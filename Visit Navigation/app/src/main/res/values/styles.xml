<resources>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/primary_dark</item>
    </style>

    <style name="AppTheme.Light" parent="android:Theme.Material.Light" />

    <!-- inherit from the material theme -->
    <style name="AppTheme.Angrest" parent="Theme.AppCompat.Light">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/accent</item>

        <item name="android:textColorPrimary">@color/titleColor</item>

        <item name="android:textColor">@color/textColor</item>

        <item name="actionBarStyle">@style/MyActionBarLogo</item>
    </style>

    <style name="MyActionBarLogo" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="background">@color/primary</item>
        <item name="logo">@drawable/ic_visit_launcher</item>
        <item name="displayOptions">useLogo|showHome|showTitle</item>
        <item name="android:title">@string/app_name</item>
    </style>
</resources>